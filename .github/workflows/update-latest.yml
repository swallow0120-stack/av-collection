name: Latest feed (not configured)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  unavailable:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '::error::The legacy feed is not validated and its schedule has been disabled. No data was changed.'
          exit 1
